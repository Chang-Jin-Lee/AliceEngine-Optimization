# 이펙트 위치

---

눈깔 오브젝트 = W_EYE
분리되면(가드브레이크 상태) 등장함

---

기준은 **현재 코드**입니다.

`Assets/Scripts/VFX/CombatVfxBridgeScript.cpp` / `Assets/Scripts/VFX/CombatVfxBridgeScript.h`

(씬 인스펙터에서 경로를 바꿔두면 실제 프리팹은 그 값으로 나옵니다.)

| 상황 | 나오는 이펙트 |
| --- | --- |
| 플레이어 라이트 공격 시 슬래시 윈도우 진입 | 슬래시 프리팹 (`slashPrefabPath`, 기본 `(01)White_Attack_SlashC.prefab`) |
| 3타(attack slot 3) 슬래시 | 슬래시 2개: 중앙 + 오른쪽(`+0.25`), 오른쪽 롤 `+20도` |
| 플레이어가 보스를 `Hit` | 히트 프리팹(`hitPrefabPath`, 기본 `(01)White_HIT.prefab`) + 스파크 오버레이(`hitOverlayPrefabPath`) |
| 플레이어가 보스를 `Hit`인데 헤비 슬롯(기본 slot 4) | 히트 프리팹만, 스파크 오버레이는 안 나옴 |
| 보스가 플레이어 `Hit` | `(00)ShockWave`만 재생, `WhiteShockBlast`는 **안 나옴** (히트만 롤백 반영) |
| 플레이어가 보스 공격 `Guard` 성공 | `(00)ShockWave` + `(00)WhiteShockBlast` + GuardRing(기본 `(05)OrangeRing.prefab`) |
| 플레이어 `GuardBreak` 당함 | `(00)ShockWave` + `(00)WhiteShockBlast` + GuardRing + GuardBreakRing(기본 `(05)OrangeDoubleRing.prefab`) |
| 플레이어 `Parry` 성공 | 스파크 오버레이 + ParryRing(기본 `(05)YellowRing.prefab`) |
| 보스가 그로기 진입하는 프레임 | BossEffectPoint(Head) 기준 RedRing(기본 `(05)RedRing.prefab`) |

추가로 현재 적용된 연출:

- 링 계열(Orange/OrangeDouble/Yellow/Red)은 시작 스케일 `1/6`에서 시작해서, 각자 `LifeTime`의 절반 시점에 프리팹 원본 스케일(1.0)에 도달하도록 커집니다.`Assets/Scripts/VFX/CombatVfxBridgeScript.cpp:39`, `Assets/Scripts/VFX/CombatVfxBridgeScript.cpp:959`

참고:

- `Guard`/`Damage` 오버레이 슬롯은 정의는 되어 있지만, 현재 `OnCombatResolve`에서 직접 호출하는 경로는 없습니다.`Assets/Scripts/VFX/CombatVfxBridgeScript.cpp:1393`, `Assets/Scripts/VFX/CombatVfxBridgeScript.cpp:1398`

---

HitPos(피격 위치)
WeaponTraceComponrnt
HealthComponent
HurtboxComponent
이 3가지로 결정됨

지금 HitPos는 **무기 트레이스 물리 히트 지점**에서 옵니다.

1. 생성 위치
    
    [WeaponTraceSystem.cpp (line 486)](https://file+.vscode-resource.vscode-cdn.net/c%3A/Users/User/.cursor/extensions/openai.chatgpt-0.4.71-universal/webview/#)
    
    sweep 히트면 hit.position을 XMFLOAT3로 넘깁니다.
    
    [WeaponTraceSystem.cpp (line 446)](https://file+.vscode-resource.vscode-cdn.net/c%3A/Users/User/.cursor/extensions/openai.chatgpt-0.4.71-universal/webview/#)
    
    정지 오버랩 fallback은 endCenter를 씁니다.
    
    그 값을 이벤트에 저장: [WeaponTraceSystem.cpp (line 413)](https://file+.vscode-resource.vscode-cdn.net/c%3A/Users/User/.cursor/extensions/openai.chatgpt-0.4.71-universal/webview/#) (ev.hitPosWS = hitPosWS)
    
2. 전투 세션에서 전달
    
    [C_CombatSessionComponent.cpp (line 4597)](https://file+.vscode-resource.vscode-cdn.net/c%3A/Users/User/.cursor/extensions/openai.chatgpt-0.4.71-universal/webview/#) 에서 frame hit 수집 후,
    
    [C_CombatSessionComponent.cpp (line 4716)](https://file+.vscode-resource.vscode-cdn.net/c%3A/Users/User/.cursor/extensions/openai.chatgpt-0.4.71-universal/webview/#) 에서 OnCombatResolvedVfx(..., hit.hitPosWS)로 그대로 넘깁니다.
    
3. VFX에서 사용
    
    [CombatVfxBridgeScript.cpp (line 1446)](https://file+.vscode-resource.vscode-cdn.net/c%3A/Users/User/.cursor/extensions/openai.chatgpt-0.4.71-universal/webview/#) OnCombatResolve의 hitPos로 받고,
    
    [CombatVfxBridgeScript.cpp (line 1466)](https://file+.vscode-resource.vscode-cdn.net/c%3A/Users/User/.cursor/extensions/openai.chatgpt-0.4.71-universal/webview/#) [SpawnHitFromResolve(hitPos, ...)](https://file+.vscode-resource.vscode-cdn.net/c%3A/Users/User/.cursor/extensions/openai.chatgpt-0.4.71-universal/webview/#)에 그대로 씁니다.
    

가드/패링 위치(플레이어의 정면 - 가드 했을 때 위치)
Player(Tia)의 자식 emptyObject
PlayerEffectPoint

보스의 정면
Boss의 자식 emptyObject
BossEffectPorint