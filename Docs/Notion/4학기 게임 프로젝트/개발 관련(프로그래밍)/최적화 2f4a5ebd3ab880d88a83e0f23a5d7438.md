# 최적화

- 렌더독 사용기
    - 타일 13 * 26 = 338개, 사이즈 (1,1,1), 디맨션 (2,2,0) 기준
    - 드로우콜이 Pass #1, Pass #2 두번 겹쳐서 나온 다는 것을 확인
        - Pass #1
            
            ![Pass #1 (1).png](%EC%B5%9C%EC%A0%81%ED%99%94/Pass_1_(1).png)
            
            ![Pass #1 (2).png](%EC%B5%9C%EC%A0%81%ED%99%94/Pass_1_(2).png)
            
            ![Pass #1 (3).png](%EC%B5%9C%EC%A0%81%ED%99%94/Pass_1_(3).png)
            
            ![Pass #1 (4).png](%EC%B5%9C%EC%A0%81%ED%99%94/Pass_1_(4).png)
            
            ![Pass #1 (5).png](%EC%B5%9C%EC%A0%81%ED%99%94/Pass_1_(5).png)
            
            ![Pass #1 (6).png](%EC%B5%9C%EC%A0%81%ED%99%94/Pass_1_(6).png)
            
            ![Pass #1 (7).png](%EC%B5%9C%EC%A0%81%ED%99%94/Pass_1_(7).png)
            
            ![Pass #1 (8).png](%EC%B5%9C%EC%A0%81%ED%99%94/Pass_1_(8).png)
            
        - Pass #2
            
            ![Pass #2 (1).png](%EC%B5%9C%EC%A0%81%ED%99%94/Pass_2_(1).png)
            
            ![Pass #2 (2).png](%EC%B5%9C%EC%A0%81%ED%99%94/Pass_2_(2).png)
            
            ![Pass #2 (3).png](%EC%B5%9C%EC%A0%81%ED%99%94/Pass_2_(3).png)
            
            ![Pass #2 (4).png](%EC%B5%9C%EC%A0%81%ED%99%94/Pass_2_(4).png)
            
            ![Pass #2 (5).png](%EC%B5%9C%EC%A0%81%ED%99%94/Pass_2_(5).png)
            
    - **동일 타일을 너무 많이, 그리고 2번** 그리고 있고, **스키닝 경로로 들어가서 64KB bones CB를 매 드로우 Map/Unmap** 하고 있었음
    - **GBuffer(불투명) + TransparentForward(불투명까지 재그림) + Outline(두 번 그리기)**
    - 위의 3개가 합쳐져서 드로우 콜이 폭증했음
        - **Pass #1 (3 Targets + Depth)**Input Layout에 BLENDINDICES/BLENDWEIGHT/SMOOTHNORMAL이 보임 → **스키닝 경로**.DrawIndexed(6) 반복 → 타일 1장씩 그리는 패턴.
        - **API Inspector에 Map/Unmap(64KB, Buffer 162)**→ 매 드로우마다 bones CB 업데이트. CPU 드라이버 오버헤드 큼.
        - **Rasterizer가 Cull Front인 샷**→ **아웃라인 패스**가 돌고 있음. 기본 outlineWidth가 0.01이라면 **모든 오브젝트가 2번 그려짐**.
        - **Pass #2 (1 Targets + Depth)**PS에 CBTransparentLight + IBL 텍스처 바인딩 → **TransparentForward 패스**.여기서도 DrawIndexed(6)이 대량 → **불투명 타일까지 다시 그림**.
    
    → 해결
    
    1. **투명 오브젝트만 TransparentForward에서 그리도록 필터링**
        - MaterialComponent에 transparent 추가
        - SkinnedDrawCommand에 transparent 추가
        - GBuffer/Shadow는 transparent==false만 렌더
        - TransparentForward는 transparent==true만 렌더
    2. **아웃라인 기본값을 0으로 변경 (기본 2배 드로우 제거)**
    - 기본 outlineWidth를 0으로 변경 → 인스턴싱 조건도 살아남음

1. **GPU 인스턴싱,** 
2. **BVH 카메라 컬링,** 
3. **쉐도우 맵 최적화,**
4.  **알파 처리 최적화**
5. G buffer 경로 줄이기
6. 쉐도우 패스 변화가 있는 것만 갱신하기

```python
네, 그 두 가지가 지금 상황에서 가장 큰 레버리지 맞습니다.
다만 “정적 메시 인스턴싱이 실제로 먹고 있는지”부터 확인한 뒤, G‑Buffer 압축으로 들어가면 시행착오가 줄어듭니다. 아래에 구체적인 진행 순서 + 변경 지점 + 설계 옵션을 정리했습니다.

1) 정적 메시 인스턴싱 (먼저 확인/적용)
현재 디퍼드 코드에는 스키닝 커맨드에 대한 인스턴싱 경로가 이미 있습니다.
타일 FBX가 이 경로에 타는지부터 확인하세요.

지금 인스턴싱 조건은 DeferredRenderSystem.cpp에서 canInstance 조건입니다.
IsRigidSkinnedCommand(cmd) AND outlineWidth <= 0 AND 인스턴싱 VS/레이아웃/버퍼 존재
IsRigidSkinnedCommand는 boneCount==1 + identity bone만 true
타일 FBX가 boneCount==0이면 인스턴싱이 안 됩니다.
해결: IsRigidSkinnedCommand를 “boneCount==0 또는 1 + identity”로 완화
GBufferSkinnedInstancedVS는 뼈를 쓰지 않으므로 boneCount==0이어도 인스턴싱 가능
배칭 키에 들어가는 항목이 많습니다(버퍼, 텍스처, rough/metal, shadingMode 등).
타일들이 같은 mesh + 같은 텍스처/머티리얼일 때만 한 번에 묶입니다
배경 타일은 outlineWidth=0로 고정하는 게 유리
적용 포인트(간단):

조건 완화: DeferredRenderSystem.cpp의 IsRigidSkinnedCommand
타일 머티리얼 통일/아웃라인 제거: 타일의 MaterialComponent
만약 타일이 스키닝 경로가 아닌 정적 경로(SimpleVertex)라면,
정적 인스턴싱을 별도로 추가해야 합니다. (이게 큰 폭 개선)

정적 인스턴싱 추가 시 핵심 작업:

GBufferInstancedVS (SimpleVertex + INSTANCE_WORLD) 추가
DeferredShader.h
인스턴스 레이아웃/버퍼 사용
DeferredRenderSystem.cpp에서 static 루프를 배치 수집 → Map → DrawIndexedInstanced로 변경
그림자 패스도 동일하게 인스턴싱 적용
DeferredRenderSystem.cpp의 RenderShadowPass
효과 기대치

같은 메시/텍스처 타일 300개 → 드로우콜을 수백 개 → 수회로 축소
Shadow/GBuffer 패스 둘 다 줄어드는 게 큽니다.
2) G‑Buffer 축소/압축 (가장 큰 대역폭 레버리지)
현재는 5 MRT + Position(128bpp)라 대역폭이 매우 큽니다.
“Position 제거 + Depth SRV로 복원”이 1순위입니다.

추천 단계 (현실적인/안전 루트)

A안: 5 → 3 RT (안전, 변경량 적음)

RT0: BaseColor(rgb) + ShadingMode(a)
DXGI_FORMAT_R8G8B8A8_UNORM_SRGB
RT1: Normal(rgb) + Roughness(a)
DXGI_FORMAT_R16G16B16A16_UNORM or FLOAT
RT2: Metalness(r) (필요시 AO 포함)
DXGI_FORMAT_R8_UNORM
Position 제거 → Depth SRV로 복원
B안: 5 → 2 RT (더 공격적, 성능 최고)

RT0: BaseColor(rgb) + ShadingMode(a)
RT1: NormalOcta(rg) + Roughness(b) + Metalness(a)
DXGI_FORMAT_R8G8B8A8_UNORM 또는 R16G16B16A16_UNORM
Position 제거 → Depth 복원
필수 변경 포인트

GBuffer 생성/카운트/바인딩
DeferredRenderSystem.h (GBufferCount)
DeferredRenderSystem.cpp (CreateGBuffer / PassGBuffer / PassDeferredLight)
Depth SRV 생성
DeferredRenderSystem.cpp에서 Depth 텍스처를 TYPELESS로 생성하고 DSV+SRV 둘 다 만들기
예: DXGI_FORMAT_R24G8_TYPELESS + DSV D24_UNORM_S8_UINT + SRV R24_UNORM_X8_TYPELESS
Shader 출력/입력 변경
DeferredShader.h
GBufferOut 구조체 갯수/채널 변경
LightPS에서 Position 대신 Depth 복원
Depth 복원 개념

g_DepthSRV를 샘플하고 InvViewProj로 월드좌표 복원
D3D NDC z는 0..1 범위이므로 z를 0..1 그대로 사용
InvViewProj는 CPU에서 계산해서 LightPS 상수버퍼에 넣기
정리

Position 제거만 해도 MRT 대역폭이 크게 줄어듭니다
Normal을 Octa로 줄이면 추가 2배 절감
지금 가장 먼저 할 일 (빠른 체크)

타일 FBX가 이미 인스턴싱 경로를 타는지 확인
IsRigidSkinnedCommand 조건 충족 여부
outlineWidth / boneCount / 같은 재질인지 체크
인스턴싱이 안 탄다면 조건을 완화하거나 정적 인스턴싱 추가
원하면 다음 중 하나 골라줘요. 바로 코드 변경으로 들어갈게요.

IsRigidSkinnedCommand 완화 + 타일 인스턴싱 확실히 타게 수정
정적 인스턴싱 경로 추가
G‑Buffer 5→3 압축 먼저 적용
G‑Buffer 5→2 + Depth 복원까지 한 번에 적용
```